/*
class Cell {
  private static cells: { [key: string]: Cell } = {};

  private constructor(private i: number, private j: number) {}

  static getCell(i: number, j: number): Cell {
    const key = `${i}-${j}`;
    if (!Cell.cells[key]) {
      Cell.cells[key] = new Cell(i, j);
    }
    return Cell.cells[key];
  }

  // Other methods and properties related to the grid cell can be added here
}

export default Cell;
*/
